# E2E 테스트

> Playwright를 이용한 End-to-End 테스트

## 📋 개요

이 디렉토리는 CampStation 프론트엔드의 E2E 테스트를 포함합니다.
Playwright를 사용하여 실제 사용자 시나리오를 검증합니다.

## 🗂️ 디렉토리 구조

```
e2e/
├── pages/              # Page Object Models
│   ├── LoginPage.ts
│   ├── CampgroundPage.ts
│   ├── ReservationPage.ts
│   └── ReviewPage.ts
├── tests/              # 테스트 파일
│   ├── home.spec.ts
│   ├── auth/
│   │   └── login.spec.ts
│   ├── campgrounds/
│   │   ├── campground.spec.ts
│   │   ├── favorite.spec.ts
│   │   └── review.spec.ts
│   └── reservations/
│       └── reservation.spec.ts
└── fixtures/           # 테스트 픽스처 (향후 추가)
```

## 🚀 실행 방법

### 모든 테스트 실행

```bash
npm run test:e2e
```

### UI 모드로 실행 (디버깅)

```bash
npm run test:e2e:ui
```

### 특정 브라우저로 실행

```bash
npm run test:e2e -- --project=chromium
```

### 특정 테스트 파일만 실행

```bash
npm run test:e2e -- tests/home.spec.ts
```

### 헤드드 모드로 실행 (브라우저 보이기)

```bash
npm run test:e2e:headed
```

### 디버그 모드로 실행

```bash
npm run test:e2e:debug
```

### 리포트 보기

```bash
npm run test:e2e:report
```

## 📝 작성된 테스트

### ✅ 홈페이지 (`home.spec.ts`)

- [x] 홈페이지가 정상적으로 로드된다
- [x] 네비게이션 메뉴가 동작한다
- [x] 하단 네비게이션이 동작한다
- [x] 반응형 레이아웃이 동작한다

### ✅ 로그인 (`auth/login.spec.ts`)

- [x] 로그인 페이지가 정상적으로 표시된다
- [x] 회원가입 페이지로 이동할 수 있다
- [x] 빈 이메일/비밀번호로 로그인 불가
- [ ] 올바른 정보로 로그인 성공 (백엔드 연동 필요)
- [ ] 잘못된 비밀번호로 로그인 실패 (백엔드 연동 필요)

### 🔄 캠핑장 (`campgrounds/campground.spec.ts`)

- [x] 캠핑장 목록 페이지가 정상적으로 표시된다
- [x] 검색 페이지가 정상적으로 표시된다
- [ ] 캠핑장 카드 클릭 → 상세 페이지 (데이터 필요)
- [ ] 캠핑장 검색 (백엔드 연동 필요)
- [ ] 예약하기 버튼 → 예약 페이지 (데이터 필요)

### 🔄 예약 (`reservations/reservation.spec.ts`)

- [x] 예약 생성 페이지가 정상적으로 표시된다
- [ ] 예약 정보 입력 및 결제 페이지 이동 (백엔드 연동 필요)
- [ ] 예약 상세 정보 표시 (데이터 필요)
- [ ] 예약 취소 (백엔드 연동 필요)

### 🔄 리뷰 (`campgrounds/review.spec.ts`)

- [ ] 리뷰 작성 페이지 표시 (데이터 필요)
- [ ] 별점 및 텍스트로 리뷰 작성 (백엔드 연동 필요)
- [ ] 이미지 업로드 (백엔드 연동 필요)
- [ ] 리뷰 수정 (데이터 필요)
- [ ] 리뷰 삭제 (백엔드 연동 필요)

### 🔄 찜하기 (`campgrounds/favorite.spec.ts`)

- [ ] 캠핑장 찜하기 (로그인 필요)
- [ ] 찜 해제 (로그인 필요)
- [ ] 찜 목록 조회 (로그인 필요)

## 📊 현재 상태

**전체 테스트**: 약 40개 (작성 완료)  
**실행 가능 테스트**: 9개  
**백엔드 연동 필요**: 31개 (`.skip` 처리)

대부분의 테스트가 실제 백엔드 API와 데이터가 필요하므로 `.skip`으로 표시되어 있습니다.
백엔드 개발 완료 후 또는 Mock 서버 구축 후 활성화할 수 있습니다.

## 🎯 테스트 전략

### Page Object Model (POM)

- 각 페이지의 UI 요소와 액션을 클래스로 캡슐화
- 테스트 코드의 유지보수성 향상
- 재사용 가능한 메서드 제공

### 테스트 구조

```typescript
test.describe("기능명", () => {
  test.beforeEach(async ({ page }) => {
    // 공통 설정
  });

  test("시나리오 설명", async () => {
    // Given: 초기 상태
    // When: 액션
    // Then: 검증
  });
});
```

### Skip 처리

백엔드 연동이 필요하거나 실제 데이터가 필요한 테스트는 `.skip`으로 표시:

```typescript
test.skip("백엔드 필요한 테스트", async () => {
  // Note: 백엔드 연동 필요
});
```

## 📚 참고 문서

- [E2E 테스트 가이드](../docs/testing/e2e-testing-guide.md)
- [Playwright 공식 문서](https://playwright.dev/)
- [테스트 전략](../docs/testing/01-test-strategy.md)

## 🔜 다음 단계

1. **Mock 서버 구축**: MSW 등을 사용하여 백엔드 없이 테스트 실행
2. **인증 픽스처**: 로그인 상태 재사용
3. **테스트 데이터 관리**: 일관된 테스트 데이터 생성
4. **CI/CD 통합**: GitHub Actions에서 자동 실행
5. **커버리지 확대**: 에러 케이스, 엣지 케이스 추가

## 🛠️ 유용한 명령어

```bash
# 특정 프로젝트만 실행
npm run test:e2e -- --project=chromium

# 특정 테스트만 실행
npm run test:e2e -- tests/home.spec.ts

# grep으로 테스트 필터링
npm run test:e2e -- --grep "로그인"

# 실패한 테스트만 재실행
npm run test:e2e -- --last-failed

# 코드 생성기 (레코딩)
npx playwright codegen http://localhost:3000
```
