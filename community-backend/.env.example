# Database Configuration
# For Docker: use 'db' as host (service name in docker-compose)
# For Local: use 'localhost'
DB_URL=jdbc:postgresql://localhost:5432/community
DB_USERNAME=postgres
DB_PASSWORD=your_password

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# API Keys
API_FOOTBALL_KEY=your_api_key_here
